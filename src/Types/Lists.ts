/**
 * Matches the type of the `maintainVisibleContentPosition` prop in the `FlashList` component.
 */
export type MaintainVisibleContentPosition = {
  disabled?: boolean;
  minIndexForVisible?: number;
  autoscrollToTopThreshold?: number;
};
