import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  // Setting buttons are used in the settings page. They may evolve more.
  setting: {
    margin: 20,
  },
});
