diff --git a/node_modules/react-native-notify-kit/android/src/main/java/app/notifee/core/ReceiverService.java b/node_modules/react-native-notify-kit/android/src/main/java/app/notifee/core/ReceiverService.java
index 736e3eb..8c1bd44 100644
--- a/node_modules/react-native-notify-kit/android/src/main/java/app/notifee/core/ReceiverService.java
+++ b/node_modules/react-native-notify-kit/android/src/main/java/app/notifee/core/ReceiverService.java
@@ -121,9 +121,12 @@ public class ReceiverService extends Service {
     NotificationModel notificationModel = NotificationModel.fromBundle(notification);
 
     // On Android 14+, users can dismiss foreground service notifications even with ongoing: true.
-    // If the dismissed notification belongs to the active foreground service, re-post it
-    // so the user remains aware of the running service.
-    if (ForegroundService.repostIfActive(notificationModel.getId())) {
+    // If the dismissed notification belongs to the active foreground service AND is explicitly
+    // marked ongoing, re-post it so the user remains aware of the running service. Notifications
+    // with ongoing:false are allowed to stay dismissed while their foreground service keeps
+    // running, matching Android 14+'s platform-supported dismissal behavior.
+    if (notificationModel.getAndroid().getOngoing()
+        && ForegroundService.repostIfActive(notificationModel.getId())) {
       return;
     }
 
